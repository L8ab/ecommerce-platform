{
  "name": "project",
  "version": "1.0.0",
  "description": "Professional project by L8ab Systems",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js || node index.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linter configured\" && exit 0",
    "build": "echo \"No build step required\" && exit 0"
  },
  "keywords": ["l8ab", "systems"],
  "author": "L8ab Systems",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
